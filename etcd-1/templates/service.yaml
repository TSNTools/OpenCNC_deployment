apiVersion: v1
kind: Service
metadata:
  name: etcd
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: client
      port: {{ .Values.service.port }}
      targetPort: 2379
    - name: peer
      port: {{ .Values.service.peerPort }}
      targetPort: 2380
  selector:
    app: etcd

